package dev.cancio.marvel_characters.presentation.screen

import org.junit.Assert.assertEquals
import org.junit.Test

class ComicFeedViewModelTest {
    @Test
    fun `WHEN onCreate THEN show a Comic list`(){
        assertEquals(4, 2 + 2)
    }
}